[build]
builder = "NIXPACKS"

[deploy]
startCommand = "node -e \"try{require('fs').unlinkSync('auth_info/.bootstrapped_from_env')}catch(e){}\" && node src/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
